{{deploy_to}}/shared/log/*.log {
  daily
  size 10M
  missingok
  compress
  rotate 20
  create 755 {{ansible_ssh_user}} {{ansible_ssh_user}}
  su {{ansible_ssh_user}} {{ansible_ssh_user}}
  date
  notifempty
  sharedscripts
  extension gz
  copytruncate
}
